<% layout('layout/boilerplate') %>
    <h1>COMPUTER BUILDS</h1>
    

    <% let types= { Budget: [], Premium: [], Gaming: [], Business: [] }; %>
        <% if (currentUser && (currentUser.role==='admin' || currentUser.role ==='employee') ) { %>
            <a class="nav-link" href="/builds/new">Create Build</a>
        <% } %>    
        <% for (let item of computerBuild) { %>
            <% types[item.category].push(item); %>
                <% } %>
                    <% for (let type in types) { %>
                        <% if (types[type].length> 0) { %>
                            <div class="row mt-5">
                                <div class="col">
                                    <h2>
                                        <%= type.toUpperCase() %>
                                    </h2>
                                </div>
                            </div>
                            <div class="row mt-3">
                                <% for (let item of types[type]) { %>
                                    <div class="col-md-3">
                                        <a href="/builds/<%= item._id %>">
                                            <div class="card mb-4">
                                                <div class="card-image">
                                                    <img src="<%= item.imgURL %>"
                                                        class="card-img-top img-fluid square-image"
                                                        alt="<%= item.name %>">
                                                </div>
                                                <div class="card-body">
                                                    <h5 class="card-title">
                                                        <%= item.name %>
                                                    </h5>
                                                    <p class="card-text">$<%= item.price %>
                                                    </p>
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                    <% } %>
                            </div>
                            <% } %>
                                <% } %>